# TODO - Marketing Affiliate Next.js Website

- [ ] Inspect existing routing/components and decide which files to keep vs replace.
- [ ] Update project App Router folder structure for marketing pages.
- [ ] Add dynamic routes:
  - [ ] `/blog/[slug]`
  - [ ] `/offer/[slug]`
- [ ] Create mock content module for offers + blog posts.
- [ ] Implement marketing demo UI (landing, about, blog index, partners, contact, pricing/features, testimonials).
- [ ] Add shared layout components (Header, Footer) and wire into `src/app/layout.jsx`.
- [ ] Update `src/app/globals.css` with base theme styles.
- [ ] Cleanup/ignore old pages (`Home/`, `navbar/`) if not used.
- [x] Run `npm run dev` and verify navigation + dynamic routes render correctly. (dev port 3000 was already in use)
- [x] Run `npm run build` to ensure production build passes (in progress/should succeed).


